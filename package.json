{
  "name": "jawabu-beauty-shop",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix jawabu-admin",
    "dev:backend": "npm run dev --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:admin": "npm run build --prefix jawabu-admin"
  }
}
